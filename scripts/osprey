#!/bin/sh
set -eu
case "${1:-}" in
    doctor) shift; exec "$(CDPATH='' cd -- "$(dirname -- "$0")" && pwd)/osprey-doctor.sh" "$@" ;;
    --help|-h) echo 'Usage: osprey doctor [options]' ;;
    *) echo 'Usage: osprey doctor [options]' >&2; exit 2 ;;
esac
